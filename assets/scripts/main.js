/**
 * WebFonts
 */
 WebFont.load({
   google: {
     families: ['Open Sans Condensed:700']
   },
   typekit: {
     id: 'vbu2xyh'
   }
 });

/**
 * Do stuff once the page has loaded!
 */
$(document).ready(function() {});
